---
title: "随机术力口频率"
slug: vocaloid
date: 2026-05-10
---

<section class="vocaloid-random-page">
<style>
    .vc-container {
        max-width: 450px;
        margin: 2rem auto;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }
    .vc-card {
        background: rgba(128, 128, 128, 0.05);
        border: 1px solid rgba(128, 128, 128, 0.2);
        border-radius: 16px;
        padding: 24px;
        backdrop-filter: blur(10px);
        transition: all 0.4s cubic-bezier(0.23, 1, 0.32, 1);
    }
    .vc-card:hover {
        transform: translateY(-5px);
        border-color: #3b82f6;
    }
    .vc-info {
        margin-bottom: 24px;
        min-height: 100px;
    }
    .vc-title {
        font-size: 1.4rem;
        font-weight: 800;
        margin: 0 0 12px 0 !important;
        line-height: 1.3;
    }
    .vc-meta {
        font-size: 0.9rem;
        opacity: 0.7;
        display: flex;
        gap: 12px;
        align-items: center;
    }
    .vc-singer-tag {
        background: rgba(59, 130, 246, 0.15);
        color: #3b82f6;
        padding: 2px 10px;
        border-radius: 6px;
        font-size: 0.75rem;
        font-weight: 700;
    }
    .vc-btn {
        width: 100%;
        background: #3b82f6;
        color: #fff !important;
        border: none;
        padding: 14px;
        border-radius: 12px;
        font-weight: 700;
        cursor: pointer;
        transition: 0.3s;
        font-size: 1rem;
    }
    .vc-btn:hover { background: #2563eb; }
    .vc-btn:disabled { opacity: 0.5; }
    
    .vc-link-btn {
        margin-top: 15px;
        display: block;
        text-align: center;
        text-decoration: none !important;
        font-size: 0.85rem;
        opacity: 0.5;
        color: inherit !important;
    }
    .vc-link-btn:hover { opacity: 1; }

    @keyframes vc-pulse {
        0% { opacity: 1; }
        50% { opacity: 0.4; }
        100% { opacity: 1; }
    }
    .loading .vc-title, .loading .vc-meta { animation: vc-pulse 1.5s infinite; }
</style>

<div class="vc-container">
    <div class="vc-card" id="vc-card">
        <div class="vc-info">
            <h3 class="vc-title" id="vc-title">正在建立同步...</h3>
            <div class="vc-meta">
                <span id="vc-pname">---</span>
                <span class="vc-singer-tag" id="vc-singer">Vocaloid</span>
            </div>
        </div>
        
        <button class="vc-btn" onclick="fetchRandomSong()" id="vc-refresh-btn">
            🔄 随机同步一首术曲
        </button>
        
        <a href="#" target="_blank" id="vc-bili-link" class="vc-link-btn" style="display:none;">去 Bilibili 观看原片 ↗</a>
    </div>
</div>

<script>
(function() {
    const WORKER_URL = 'https://vocaloid-api.925857461.workers.dev/';

    window.fetchRandomSong = async function() {
        const card = document.getElementById('vc-card');
        const btn = document.getElementById('vc-refresh-btn');
        const title = document.getElementById('vc-title');
        const pname = document.getElementById('vc-pname');
        const singer = document.getElementById('vc-singer');
        const link = document.getElementById('vc-bili-link');

        card.classList.add('loading');
        btn.disabled = true;
        btn.innerText = "同步中...";

        try {
            const response = await fetch(WORKER_URL);
            const data = await response.json();

            if (data.error) throw new Error(data.error);

            title.innerText = data.title;
            pname.innerText = "P主: " + (data.p_name || "未知");
            singer.innerText = data.singer || "Vocaloid";
            
            link.href = data.link;
            link.style.display = 'block';

        } catch (err) {
            console.error(err);
            title.innerText = "信号中断";
            pname.innerText = "请重试或检查 API";
        } finally {
            card.classList.remove('loading');
            btn.disabled = false;
            btn.innerText = "🔄 随机同步一首术曲";
        }
    };

    // 自动触发一次
    document.addEventListener('DOMContentLoaded', window.fetchRandomSong);
})();
</script>
</section>
